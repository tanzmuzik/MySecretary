// Preload script for security
// This file runs in a separate context with access to both Node.js and browser APIs
// Currently empty but required for contextIsolation

window.addEventListener('DOMContentLoaded', () => {
  console.log('Clock Widget Loaded');
});
